.archive/

CLAUDE.md
README.md

old_*/**

# Windows only
{{ if ne .chezmoi.os "windows" }}

    .chezmoiscripts/windows/**
    AppData/**
    .ansible/tasks/windows

{{ end }}

# Unix-like only
{{ if not (or (eq .chezmoi.os "darwin") (eq .chezmoi.os "linux")) }}
    .ansible
    .chezmoiscripts/*.sh
    .config
    .gnupg
    .local
    .p10k.zsh
    .zshrc
    .zshrc_custom
    git
{{ end }}

# Linux only
{{ if ne .chezmoi.os "linux" }}

    .ansible/tasks/linux
    .ansible/tasks/arch
    .ansible/tasks/pop
    .ansible/tasks/fedora
    .ansible/tasks/kinoite
    .ansible/tasks/aurora
    .chezmoiscripts/linux/**
    .chezmoiscripts/linux/arch/**
    .chezmoiscripts/linux/pop/**
    .chezmoiscripts/linux/fedora/**
    .chezmoiscripts/linux/kinoite/**
    .chezmoiscripts/linux/aurora/**

{{ end }}

# Arch-only configs (Chrome/font rendering tuned for Arch)
{{ if not (and (eq .chezmoi.os "linux") (eq .os.idLike "arch")) }}

    .config/fontconfig
    .config/chrome-flags.conf
    .config/environment.d

{{ end }}

# macOS only
{{ if ne .chezmoi.os "darwin" }}

    .ansible/tasks/darwin
    .chezmoiscripts/darwin/**
    .config/iterm2
    .aerospace.toml
    Library

{{ end }}

{{ if eq .chezmoi.os "linux" }}

{{ if ne .os.idLike "arch" }}
    .chezmoiscripts/linux/arch/**
    .ansible/tasks/arch
{{ end }}
{{ if ne .os.idLike "pop" }}
    .chezmoiscripts/linux/pop/**
    .ansible/tasks/pop
    .config/cosmic
{{ end }}
{{ if ne .os.idLike "fedora" }}
    .chezmoiscripts/linux/fedora/**
    .ansible/tasks/fedora
{{ end }}
{{ if ne .os.idLike "kinoite" }}
    .chezmoiscripts/linux/kinoite/**
    .ansible/tasks/kinoite
{{ end }}
{{ if ne .os.idLike "aurora" }}
    .chezmoiscripts/linux/aurora/**
    .ansible/tasks/aurora
{{ end }}

{{ end }}
