# Fedora Kinoite task chain.
#
# Layering model:
# - Host (rpm-ostree): shell + tmux + CLI tools + editor + dev runtimes.
#   Every new layer requires reboot; host-packages stops the play early if
#   it had to layer something so the user reboots once and re-runs apply.
# - Flatpak: all GUI apps.
# - HOME installers: vendor scripts that write into $HOME (Claude Code via
#   the official curl | bash installer, lands in ~/.local/bin, self-updates).
#
# Aurora (ublue-os KDE remix of Kinoite) has its own tree at tasks/aurora/ —
# it uses brew for the dev CLI, skips the Flathub remote-add (preconfigured
# in the base image), and installs Claude Code via brew. Kept separate for
# explicit visibility even though ~14 Flatpak tasks are identical.

- import_tasks: flathub.yml
- import_tasks: host-packages.yml
- import_tasks: fonts.yml
- import_tasks: claude-code.yml

- import_tasks: keepassxc.yml
- import_tasks: google-chrome.yml
- import_tasks: telegram.yml
- import_tasks: spotify.yml
- import_tasks: obsidian.yml
- import_tasks: ticktick.yml
- import_tasks: insync.yml
- import_tasks: vscode.yml
{{ if eq .hosttype "work" }}
- import_tasks: bruno.yml
- import_tasks: dbeaver.yml
{{ end }}
{{ if eq .hosttype "home" }}
- import_tasks: steam.yml
{{ end }}

- import_tasks: tmux-plugins.yml
- import_tasks: neovim-plugins.yml
