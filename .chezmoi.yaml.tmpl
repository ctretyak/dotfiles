{{- $choices := list "home" "work" -}}
{{- $hosttype := promptChoice "What type of host are you on" $choices -}}

diff:
  exclude:
    - scripts

data:
  os:
    idLike: {{ or (get .chezmoi.osRelease "idLike") (get .chezmoi.osRelease "id") }}
  hosttype: {{ $hosttype }}
  wsl: false
